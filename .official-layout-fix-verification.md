Official layout fix verification 22 Sep 2026: generator now embeds the uploaded logo as a base64 JPEG inside the downloaded document, so the header image is present without relying on an external storage URL. Signature layout uses a two-column grid with stable role/name spacing for 1–4 signers. Copies are normalized to remove existing manual prefixes and rendered in an ordered list without duplicate numbering. TypeScript check, 8 Vitest tests, and production build pass.
